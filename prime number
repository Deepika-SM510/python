b=int(input("enter a number: "))
for a in range(1,b+1):
    if a>1:
        is_prime=True
        for i in range(2,a):
                if a%i==0:
                    is_prime=False
                    break
        if is_prime:
            print(a)
